name: Validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  distributed-license:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Verify packaged license terms
        run: cmp LICENSE skills/license-boundary/LICENSE.txt
